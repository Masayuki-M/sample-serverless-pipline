version: 0.1
phases:
  install:
    commands:
    - npm install time
    - aws cloudformation package --template-file samTemplate.yaml --s3-bucket lambda-function-src-20170620 --output-template-file NewSamTemplate2.yaml
artifacts:
  type: zip
  files:
    - NewSamTemplate2.yaml
