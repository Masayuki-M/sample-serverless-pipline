version: 0.1
phases:
  install:
    commands:
    - npm install time
    - aws cloudformation package --template-file samTemplate.yml --output-template-file NewSamTemplate.yml --s3-bucket lambda-function-src-20170620
    artifacts:
      type: zip
      files:
        - NewSamTemplate.yml
